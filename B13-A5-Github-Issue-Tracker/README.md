
    - 1️⃣ What is the difference between var, let, and const?
Ans:var is the old way to declare variables. can be declared, reassigned,hoisted.

    let is block scoped, can not redeclared, can reassign, hoisted but in temporal dead zone.

     const is block scoped, can not redeclared,can not reassign.
    - 2️⃣ What is the spread operator (...)?

    Ans: The spread operator is used to expand elements of an array, object, or iterable into individual elements.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?

    Ans:map() creates a new array by transforming each elements.
        filter() creates a new array with elements that pass a condition.
        forEach simply loops through an array.

    - 4️⃣ What is an arrow function?

    Ans: An arrow function is a shorter way to write a function ai js introduced in ES6.
    - 5️⃣ What are template literals?

    Ans: The template literals are a feature in js that allow you to create strings more easily using bacticks instead of single or double quote.




